services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-spiral}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-spiral}
      POSTGRES_DB: ${POSTGRES_DB:-spiral}
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
